# Expo HAS CHANGED

This app targets **Expo SDK 54** (react-native 0.81). Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code. Keep native module versions in sync with `npx expo install` (never pin to SDK 57+ packages).
